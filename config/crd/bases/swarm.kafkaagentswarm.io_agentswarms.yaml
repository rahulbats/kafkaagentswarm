---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: agentswarms.swarm.kafkaagentswarm.io
spec:
  group: swarm.kafkaagentswarm.io
  names:
    kind: AgentSwarm
    listKind: AgentSwarmList
    plural: agentswarms
    singular: agentswarm
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AgentSwarm is the Schema for the agentswarms API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of AgentSwarm
            properties:
              kafka:
                description: foo is an example field of AgentSwarm. Edit agentswarm_types.go
                  to remove/update
                properties:
                  brokers:
                    items:
                      type: string
                    type: array
                required:
                - brokers
                type: object
              nodes:
                items:
                  description: AgentNode defines a single DAG node execution unit
                  properties:
                    allowedTools:
                      items:
                        type: string
                      type: array
                    dependsOn:
                      items:
                        type: string
                      type: array
                    image:
                      type: string
                    instructions:
                      type: string
                    llm:
                      description: |-
                        llm configures the OpenAI-compatible endpoint this node's agent loop
                        calls. Defaults to a local Ollama server if left unset.
                      properties:
                        apiKeySecretRef:
                          description: |-
                            apiKeySecretRef optionally references a Secret key holding a bearer
                            token for baseURL. Local servers (Ollama, MLX, ...) usually don't need
                            one; set this for a hosted OpenAI-compatible provider that does.
                          properties:
                            key:
                              description: The key of the secret to select from.  Must
                                be a valid secret key.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the Secret or its key must
                                be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                        baseURL:
                          default: http://localhost:11434/v1
                          description: |-
                            baseURL is an OpenAI-compatible chat-completions endpoint. Defaults to
                            a local Ollama server; point this at any OpenAI-compatible endpoint
                            (a local MLX server, a hosted provider that speaks the OpenAI API,
                            etc.) to use something else.
                          type: string
                        model:
                          default: llama3.1
                          description: model is the model name requested from baseURL.
                          type: string
                      type: object
                    mcpServices:
                      items:
                        description: MCPEndpoint defines shared MCP server service
                          connections
                        properties:
                          name:
                            type: string
                          url:
                            type: string
                        required:
                        - name
                        - url
                        type: object
                      type: array
                    name:
                      type: string
                    replicas:
                      format: int32
                      type: integer
                    type:
                      description: NodeType identifies the execution pattern of a
                        swarm node
                      type: string
                  required:
                  - name
                  type: object
                type: array
            required:
            - nodes
            type: object
          status:
            description: status defines the observed state of AgentSwarm
            properties:
              activeNodes:
                format: int32
                type: integer
              conditions:
                description: |-
                  conditions represent the current state of the AgentSwarm resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
